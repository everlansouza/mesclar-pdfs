import React from 'react';

const Header = () => {
    return (
        <header className="bg-blue-600 h-16 flex items-center justify-center">
            <img src="/logo-powerpdf.png" alt="Logo" />
        </header>
    );
};

export default Header;