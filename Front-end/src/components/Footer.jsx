import React from 'react';

const Footer = () => {
    return (
        <footer className="flex items-center justify-center mb-4">
            <img src="/logo-powercrm.png" alt="Logo" />
        </footer>
    );
};

export default Footer;